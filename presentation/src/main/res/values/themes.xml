<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.GetUp" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowBackground">@color/mainBackground</item>
        <item name="android:colorBackground">@color/mainBackground</item>
        <item name="android:textColor">@color/mainForIcons</item>
        <item name="colorPrimaryDark">@color/mainBackground</item>
        <item name="android:navigationBarColor">@color/mainBackground</item>
    </style>

    <style name="Theme.GetUp" parent="Base.Theme.GetUp" />
</resources>